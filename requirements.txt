# Tweedle backend dependencies
#
# Django 5.2 is the current Long-Term Support (LTS) release line
# (released April 2025, supported through April 2028). We pin to the
# 5.2.x line so we keep receiving LTS security/bugfix point releases
# without jumping to a non-LTS minor.
Django>=5.2,<5.3

# PostgreSQL driver (local dev DB switched to Postgres).
psycopg[binary]>=3.2

# Loads DB_* credentials from a root .env file (kept out of version control).
python-dotenv>=1.0

# Authentication (local username/email + password; social/MFA deferred).
django-allauth>=65,<66
